import AuthPlaceholder from '../components/AuthPlaceholder.jsx';

export default function RegisterPage() {
  return (
    <AuthPlaceholder
      title="Criar conta"
      description="Tela de cadastro preparada para receber o HTML base."
      actionLabel="Cadastrar"
    />
  );
}
